/**
 * ARENA Session Dashboard
 * Thin re-export of the existing SparcDashboard (operational control center).
 * Keeps a single dashboard implementation under the ARENA route.
 */
export { default } from "@/pages/sparc/SparcDashboard";
